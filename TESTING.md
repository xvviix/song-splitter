# راهنمای تست XVVIIX

دو حالت داریم. حالت ۱ همین الان قابل تست است، حالت ۲ روی کامپیوتر خودتان.

---

## حالت ۱ — موتور مرورگری (⚡ Fast)

**همین الان و بدون هیچ نصبی قابل تست است.**

1. پیش‌نمایش زنده (پورت ۸۰۰۰) را باز کنید.
2. یک آهنگ بکشید داخل کادر یا روی «browse files» بزنید.
3. مطمئن شوید گزینه‌ی **⚡ Fast** انتخاب است.
4. دکمه‌ی سبز **Split into 4 stems** را بزنید.

### چه چیزی باید ببینید

| مرحله | انتظار |
|---|---|
| بعد از انتخاب فایل | نام آهنگ، حجم، پخش‌کننده و دکمه‌های Replace/Remove |
| حین پردازش | نوار درصد که جلو می‌رود، «Separating stems…» |
| زمان | حدود ۷ برابر سریع‌تر از طول آهنگ (آهنگ ۳ دقیقه‌ای ≈ ۳۰ ثانیه) |
| بعد از اتمام | کارت **Separated stems** با ۴ پخش‌کننده |

### تست کیفیت جداسازی

بهترین آزمون گوش دادن است:

- **drums** → فقط ضرب و کوبه‌ای، بدون ملودی کشیده
- **bass** → فقط فرکانس‌های پایین
- **vocals** → صدای خواننده (با کمی نشتی از سازهای وسط)
- **other** → گیتار، کیبورد و بقیه

> ⚠️ انتظار کیفیت Demucs را نداشته باشید. این پردازش سیگنال کلاسیک است، نه هوش مصنوعی. نشتی بین لایه‌ها طبیعی است.

### تست میکسر (Master editor)

1. از نوار کناری **Master editor** را بزنید.
2. **▶** را بزنید — همه‌ی لایه‌ها باید **هم‌زمان و سینک** پخش شوند.
3. روی **M** کنار vocals بزنید → آهنگ کارائوکه می‌شود.
4. روی **S** کنار drums بزنید → فقط درام.
5. اسلایدر **PAN** را بچرخانید → صدا بین چپ و راست جابه‌جا شود.
6. **Export mix as WAV** → فایل میکس دانلود شود.
7. **Reset all** → همه‌چیز صفر شود **و آهنگ به اول برگردد**.

---

## حالت ۲ — موتور محلی (✦ Best)

**این را باید روی کامپیوتر خودتان تست کنید، نه اینجا.**

### پیش‌نیاز
پایتون ۳.۱۰ تا ۳.۱۲ از [python.org](https://www.python.org/downloads/)
هنگام نصب حتماً تیک **Add Python to PATH** را بزنید.

### مراحل

1. در سایت گزینه‌ی **✦ Best** را انتخاب کنید.
2. چون موتور نصب نیست، پنجره‌ی نصب **خودکار** باز می‌شود.
3. دکمه‌ی **⊞ Windows** (یا **⌘ macOS / Linux**) را بزنید.
4. **دو فایل** دانلود می‌شود:
   - `xvviix_engine.py`
   - `install-xvviix.bat`
5. **هر دو را در یک پوشه‌ی خالی بگذارید** (مثلاً `Desktop\xvviix`).
6. روی `install-xvviix.bat` دوبار کلیک کنید.

### چه چیزی باید در پنجره‌ی سیاه ببینید

```
 [1/4] بررسی پایتون...
       ✅ پایتون 3.12.x پیدا شد
 [2/4] ساخت محیط مجزا...
       ✅ ساخته شد
 [3/4] نصب موتور هوش مصنوعی...
       (چند دقیقه طول می‌کشد — حدود ۲ گیگ دانلود)
       ✅ نصب کامل شد
 [4/4] اجرای موتور پردازش...

   XVVIIX — موتور پردازش محلی
   دستگاه پردازش : NVIDIA GeForce RTX ...   ← یا CPU
   آدرس          : http://127.0.0.1:8765

   ✅ آماده است. حالا به سایت برگردید.
```

7. **پنجره را نبندید.** به سایت برگردید.
8. سایت هر ۳ ثانیه خودکار چک می‌کند و باید نشان دهد:
   `Connected · GPU: RTX 4060` یا `Connected · CPU`
9. حالا آهنگ را Split کنید — این بار با Demucs واقعی.

### زمان مورد انتظار

| سخت‌افزار | آهنگ ۳ دقیقه‌ای |
|---|---|
| کارت گرافیک NVIDIA | ۲۰ تا ۴۰ ثانیه |
| فقط CPU | ۳ تا ۱۰ دقیقه |

### تست fallback (مهم)

قبل از نصب، گزینه‌ی **✦ Best** را انتخاب کنید و بدون نصب کردن، یک آهنگ را Split کنید.
باید **خودکار برگردد** به موتور مرورگری با پیام:
> «Local engine unavailable — using the browser engine…»

یعنی هیچ‌وقت کار کاربر شکست نمی‌خورد.

### حذف کامل
فقط پوشه را پاک کنید. هیچ جای دیگری از سیستم دست نخورده.

---

## چک‌لیست خطاها

| مشکل | علت | راه‌حل |
|---|---|---|
| «پایتون نصب نیست» | پایتون نیست یا در PATH نیست | نصب مجدد با تیک Add to PATH |
| نصب وسط کار قطع شد | اینترنت | دوباره فایل bat را اجرا کنید، از همان‌جا ادامه می‌دهد |
| سایت وصل نمی‌شود | پنجره بسته شده | دوباره bat را بزنید |
| مرورگر جلوی اتصال را گرفت | سایت HTTPS، موتور HTTP | روی کروم مشکلی نیست؛ در فایرفاکس/سافاری ممکن است بلاک شود |
| Split دکمه‌اش خاکستری است | فایل انتخاب نشده یا هیچ لایه‌ای تیک ندارد | فایل بدهید و حداقل یک لایه تیک بزنید |

---

## تست‌هایی که من انجام دادم ✅

- سایت و همه‌ی فایل‌ها با کد ۲۰۰ سرو می‌شوند
- `splitter.worker.js` با نوع صحیح `text/javascript`
- موتور محلی اجرا شد و `/api/health` جواب داد
- CORS از دامنه‌ی سایت اجازه می‌دهد
- بازسازی کامل سیگنال: مجموع ۴ لایه دقیقاً برابر آهنگ اصلی (خطا = 0.000000)
- جداسازی درام: نسبت ۱۷۵ برابر بین ضربه و بخش کشیده
- سرعت: ۷.۲ برابر سریع‌تر از زمان واقعی
- سینتکس همه‌ی فایل‌ها معتبر، هیچ id تکراری، هیچ ارجاع خالی

### چیزی که نتوانستم تست کنم ❌
- خود **Demucs** (نصب PyTorch حدود ۲ گیگ است و اینجا جا نمی‌شود)
- فایل `.bat` روی **ویندوز واقعی** (اینجا لینوکس است)

اگر در این موارد به خطا خوردید، متن خطا را بفرستید تا درست کنم.
